import { HelpSupportContent } from "@/components/shared/help-support-content";

export const metadata = {
  title: "Help / Contact Support",
};

export default function HelpPage() {
  return <HelpSupportContent linkBase="" />;
}
